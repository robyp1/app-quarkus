package org.acme;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeApplicationResourceIT extends ApplicationResourceTest {

    // Execute the same tests but in native mode.
}